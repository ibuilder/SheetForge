{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updates",
  "description": "Check for, download and apply a signed update, and restart into it. Signature verification happens in the updater plugin against the public key in tauri.conf.json; an unsigned or mis-signed payload is discarded rather than applied. Desktop only — mobile updates come from the app stores.",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
