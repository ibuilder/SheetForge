{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "exporter",
  "description": "Follow a hyperlink found in a drawing, by handing it to the system browser. Restricted to http and https, so a document cannot navigate the application itself or reach a custom scheme handler. Saving an export needs no permission here: the destination is chosen by a native dialog on the Rust side and no path crosses the boundary.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://*" }, { "url": "http://*" }]
    }
  ]
}
